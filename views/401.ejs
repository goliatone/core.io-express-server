<div class="panel">
    <h1>Unauthorized</h1>
    <h2>
    <!-- We should have a production vs development strategy -->
    <% if (typeof message !== 'undefined') { %>
        <%= message %>
    <% } else { %>
        You need to log in.
    <% } %>
    </h2>

    <% if (flash) {
        Object.keys(flash).map((key)=>{
    %>
            <p><%= flash[key] %></p>
    <%  });
    } %>

    <p class="help">
        <a href="http://en.wikipedia.org/wiki/HTTP_401">Why</a> might this be happening?
    </p>
</div>
