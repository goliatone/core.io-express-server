<div class="panel error">
    <h1>404... Something's fishy here.</h1>
    <h2>
    <% if (typeof error !== 'undefined') { %>
        <%- error %>
    <% } else { %>
        The page you were trying to reach doesn't exist.
    <% } %>
    </h2>
    <p class="help">
        <a href="http://en.wikipedia.org/wiki/HTTP_404">Why</a> might this be happening?
    </p>
</div>
